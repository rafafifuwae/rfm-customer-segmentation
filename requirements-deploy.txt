# Dependencies minimal untuk runtime (FastAPI + Streamlit + predictor).
# Skip training-only deps (jupyter, plotly, seaborn, kneed, tabulate).

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0
pyyaml>=6.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9

# UI
streamlit>=1.31.0
requests>=2.31.0
